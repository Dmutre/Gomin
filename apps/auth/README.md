# Auth Service

This service responsible for user and cross microservice authorization. It holds sensitive data about user (username, password, session data). And manage permissions between microservices to perform some actions.

