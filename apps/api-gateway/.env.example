NODE_ENV=development
PORT=3000
HOST=localhost

REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password

AUTH_SERVICE_URL=localhost:5000
COMMUNICATION_SERVICE_URL=localhost:5001

# Service identity — must match what was seeded in the auth service DB
SERVICE_NAME=gateway-service
SERVICE_SECRET=your_gateway_service_secret
