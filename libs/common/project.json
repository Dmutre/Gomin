{
  "name": "common",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/common/src",
  "projectType": "library",
  "tags": [],
  "// targets": "to see all targets run: nx show project common --web",
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "dependsOn": [
        {
          "target": "generate-types",
          "projects": ["permission-db"]
        }
      ]
    }
  }
}
